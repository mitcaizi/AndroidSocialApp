<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lab_7">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PhotoPreview" android:theme="@style/Theme.home"></activity>
        <activity android:name=".PersonToFollow" android:parentActivityName=".HomeActivity"/>
        <activity android:name=".EditProfile" android:theme="@style/Theme.Splash" />
        <activity android:name=".SignupLogin" android:theme="@style/Theme.Splash"> </activity>
        <activity android:name=".HomeActivity" android:theme="@style/Theme.home"/>
        <activity android:name=".MainActivity" android:theme="@style/Theme.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>